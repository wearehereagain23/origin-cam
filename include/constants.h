#pragma once
#include <wx/colour.h>

namespace Theme
{
    const wxColour BACK = wxColour(15, 15, 15);
    const wxColour PANEL = wxColour(25, 25, 25);
    const wxColour REF = wxColour(35, 35, 35);
    const wxColour ACCENT = wxColour(124, 58, 237);
    const wxColour TEXT_DIM = wxColour(150, 150, 150);
}